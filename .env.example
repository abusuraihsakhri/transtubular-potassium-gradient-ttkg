# Copy this file to .env and set a secure key for production
# Generate a secure key with: python -c "import secrets; print(secrets.token_hex(32))"
AUDIT_SECRET_KEY=replace-with-secure-random-key
MODEL_PROVIDER=mock
